# S12 — Draft-night board

**Status:** NOT STARTED
**Depends on:** S7, S10 operator note
**Worktree:** `git worktree add ../vorpal-s12 -b feat/board-night main`
**Owns:** `src/vorpal/board/**`, snapshots
**Must not touch:** valuation, evals, CLI wiring except banners you already
receive.

---

## Why this exists

S7 shipped a correct dump of the payload. S10 put it in front of a
human on a pick timer. The human could not use it.

The last live page had ten red banners above the rec, a rec heading
that was a host id, a recommendation for a pick that had already
passed, eighteen identical empty-week rows, and a table whose headers
were field names. Spec said banners should be loud. Loud is not "cover
the pick."

## Copy-paste prompt

```
Read docs/SPEC.md section 6, docs/handoffs/S7.md, docs/handoffs/S10.md
(the page-failed-the-human section), and evals/REPORT.md "Operator note".

The pick the operator can act on is the first thing on the page: name,
position, slot, one-line why, two alternatives. Everything else is
secondary.

1. Night view vs dump. Scoring-key itemization, borrowed-league prose,
   keepers_possible, board_capped, and raw PlatformError JSON do not
   belong at 1.5rem red above the rec. One compact status line can name
   that banners exist. The rec stays on screen at 30s.

2. Never show a host id as the rec heading. If the proposal is not on
   the current board, say the rec is stale and show the calculator
   name, not the number.

3. Weekly vector: do not print 18 copies of the same empty-slot list.
   Show holes and bye weeks only.

4. The pick timer is a countdown against age, or it is omitted. A
   static "Pick timer: 30s" is a setting, not a clock.

5. Column headers a human can read in one glance. ADP 0.0 from a
   capped forecast should look empty, not like a rank.

Keep the page one HTML file, meta-refresh, no framework. Snapshots
must change. TDD: FAIL_TO_PASS on the layout you are replacing.
```
