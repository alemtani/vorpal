# S1 — Sleeper documented reads

**Status:** NOT STARTED
**Depends on:** S0 merged
**Worktree:** `git worktree add ../vorpal-s1 -b feat/sleeper main`
**Owns:** `src/vorpal/sleeper/**`, `tests/sleeper/**`
**Must not touch:** `contracts.py`, `errors.py`, `pyproject.toml`, any other
subpackage
**Spec:** §2 (endpoints), §6 (poll and backoff), §3 sources table

---

## Copy-paste prompt

```
Read docs/SPEC.md, docs/PLAN.md, docs/handoffs/S0.md, then docs/prompts/S1.md.

Build src/vorpal/sleeper/ — the documented api.sleeper.app reads. Nothing else.

git worktree add ../vorpal-s1 -b feat/sleeper main

Scope:
- GET /draft/{id}, GET /draft/{id}/picks, GET /league/{id}, GET /user/{name_or_id},
  GET /players/nfl.
- Split transport from parsing. A parse function takes a dict from a fixture and
  returns a contracts type; it never touches the network. Transport is one thin
  client class. This split is what lets you hit 99% coverage without mocking
  everything.
- /players is ~5MB: cache to disk, at most one fetch per day, and the cache path
  is injectable so tests never write to a real home directory.
- Stay under 1000 calls/min. Error backoff 5s, 15s, 45s, hold; reset on success.
  Backoff is a pure schedule function you can test without sleeping.
- Map HTTP and shape failures to PlatformError. Never return a partial object.
- draft.status is the source of truth for state, not start_time. Observed values:
  pre_draft, drafting, complete.
- Do NOT filter /players by active=true. search_rank is not ADP and you must not
  return it as one.

Out of scope, and someone else owns it: the projections host and FantasyPros
(S2), any interpretation of slots or scoring (S3), the poll loop itself (S7 owns
the loop; you own the client it calls).

Test against tests/fixtures/ only. Use respx for transport tests. No live calls.

TDD: failing test first.

Finish with the four handoff steps in docs/PLAN.md section 5. docs/handoffs/S1.md
must give S8 the exact client constructor and method signatures.
```

## Definition of done

- Every endpoint has a parse test against a recorded fixture.
- Backoff schedule tested without wall-clock sleeping.
- `/players` cache proven to fetch once across two calls in one day.
