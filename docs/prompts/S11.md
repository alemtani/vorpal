# S11 — Paid forecast, human replay, operator mock

**Status:** NOT STARTED
**Depends on:** S10 report
**Worktree:** `git worktree add ../vorpal-s11 -b feat/eval-live main`
**Owns:** `evals/REPORT.md` update, live rehearsal notes
**Must not touch:** `src/` internals unless a paid-key bug is in ingest.

---

## Why this exists

S10 measured the golden set and showed the model separates from
`argmax_vols`. Three things it could not measure, all blocked on
operator-held secrets or a live mock:

1. FantasyPros in this environment is the public 10-row cap. Live
   boards had 60 projected players. That is not a draft.
2. Anthropic credits ran out before the two operator mocks and before a
   second dress rehearsal.
3. The mock id S10 attached was already drafting; the operator said it
   was not theirs. The poll was stopped.

## Copy-paste prompt

```
Read docs/SPEC.md, docs/PLAN.md, docs/handoffs/S10.md, evals/REPORT.md.

Do not retune gates. S10's golden table already separates.

1. Confirm a paid FantasyPros key (lists longer than 10 rows, OP ADP
   non-empty for superflex). Re-run regret and the two human drafts
   from evals/human_drafts.py against that forecast. Keep the S10
   four-column table shape. Do not fold skips into pass rates.

2. Anthropic must accept recommend / run_stability. If credits are
   still out, stop.

3. Dress rehearsal: operator starts a mock and sends draft id +
   username + scoring-league-id if league_id is null. Attach
   evals/rehearse.py. Time cold start and on-clock model calls against
   draft.pick_timer. Confirm the page is readable before the clock
   hits zero.

Update evals/REPORT.md. Do not hide the S10 golden result. Add a
section that says what the paid key changed.
```
