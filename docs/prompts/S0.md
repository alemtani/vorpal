# S0 — Seed: contracts, errors, fixtures, CI

**Status:** NOT STARTED
**Depends on:** nothing. This blocks every other session.
**Worktree:** `git worktree add ../vorpal-s0 -b chore/seed main`
**Owns:** `pyproject.toml`, `.github/`, `src/vorpal/contracts.py`,
`src/vorpal/errors.py`, `src/vorpal/__init__.py`, `tests/fixtures/`,
`tools/record_fixtures.py`, `AGENTS.md`
**Must not touch:** any other `src/vorpal/` subpackage. Do not implement
features. This session ships types and infrastructure only.

---

## Copy-paste prompt

```
Read docs/SPEC.md and docs/PLAN.md in full, then docs/prompts/S0.md.

You are the seed session for vorpal. You block seven other sessions, so your
job is to be complete and correct, not to be fast. Ship no feature logic.

Work in a git worktree: git worktree add ../vorpal-s0 -b chore/seed main

Do this in order.

1. Record fixtures FIRST, before you write a single type. tools/record_fixtures.py
   reads the documented Sleeper endpoints (draft, league, user, picks, players),
   the undocumented projections endpoint, and FantasyPros consensus-rankings,
   and writes redacted JSON under tests/fixtures/. Redact on the way in: no
   manager display names, no league names, no user ids beyond the operator's own
   synthetic one. Include at least: one league-attached snake redraft, one
   standalone mock with league_id null, one superflex league, one league with a
   K or DST slot, and one mid-draft picks list. If you cannot reach an endpoint,
   say so in the handoff and hand-write the fixture from the documented shape
   with a comment marking it unverified.

2. Write src/vorpal/contracts.py from the recorded bytes, not from the spec
   prose. Frozen dataclasses, slots=True. It must cover every structure that
   crosses a module boundary: LeagueConfig, Seat, Slot codes, Player, StatRow,
   EcrRow, OverrideRow, Pick, DraftState, Replacement, WeeklyCell, BoardRow,
   Payload, Proposal, Flag, GateResult, Banner. Payload and Proposal must
   serialise to exactly the JSON in SPEC.md section 4 — write a test that
   asserts the key sets match.

3. Write src/vorpal/errors.py: VorpalError, and under it UnsupportedLeague
   (permanent), DataRefusal (fixable by a better file), PlatformError (the API),
   UserRefusal. Do not collapse the refusal types. Each carries a message that
   is safe to print to stderr.

4. pyproject.toml with EVERY dependency v1 will need, so no downstream session
   ever edits this file: httpx, anthropic, pytest, pytest-cov, respx, ruff.
   Python >=3.12. Pytest markers: invariant, golden, live. Coverage gate
   --cov-fail-under=99. uv for the toolchain.

5. .github/workflows/ci.yml: 3.12 and 3.13, ruff check, ruff format --check,
   pytest with the coverage gate. The live marker is deselected in CI.

6. Rewrite AGENTS.md. The current one is stale against SPEC.md: it says VORP
   where the spec says VOLS, describes survival bands and a placeholder sigma
   that SPEC.md section 4 explicitly removed, and cites section numbers (§7, §9)
   that no longer exist. Fix all of it against the current spec, and add the
   session protocol from docs/PLAN.md section 5 as a standing rule.

TDD per AGENTS.md: write the failing test first, confirm it fails for the reason
you intend, then implement.

Before you open the PR, do all four handoff steps in docs/PLAN.md section 5.
In particular, docs/handoffs/S0.md must list every contract type with its real
field names, because the other seven sessions will read that file instead of
guessing.
```

## Definition of done

- `uv run pytest` green, coverage gate passing.
- `tests/fixtures/` has the six scenarios above.
- `docs/handoffs/S0.md` lists every contract type and its fields.
- AGENTS.md contains no reference to VORP-as-v1-value, survival bands, or
  section numbers that do not exist in SPEC.md.
