# S3 — Resolve: slots, scoring, seat, refusals

**Status:** NOT STARTED
**Depends on:** S0 merged
**Worktree:** `git worktree add ../vorpal-s3 -b feat/resolve main`
**Owns:** `src/vorpal/resolve/**`, `tests/resolve/**`
**Must not touch:** `contracts.py`, `errors.py`, `pyproject.toml`, any other
subpackage
**Spec:** §2 entire

---

## Copy-paste prompt

```
Read docs/SPEC.md, docs/PLAN.md, docs/handoffs/S0.md, then docs/prompts/S3.md.

Build src/vorpal/resolve/ — SPEC.md section 2, all of it, and nothing else.
Zero network. Every function takes already-parsed dicts or contracts types.

git worktree add ../vorpal-s3 -b feat/resolve main

Resolve four things and produce banners for each:

1. Slots, once. League roster_positions if the draft belongs to a league; else
   draft.settings.slots_*. Infer bench only when slots_bn is absent AND the
   league list did not fire: rounds minus starter slots. A borrowed scoring
   league NEVER overrides slots. Eligibility table is in the spec; encode it.

2. Scoring source. draft.league_id set means read that league. league_id null
   (standalone mock) means borrow the named scoring league and banner both, and
   say plainly in the banner that they may disagree. metadata.scoring_type is a
   label, not a table — do not use it as one.

3. Seat. Match the operator user_id in draft_order. Not picked_by, which is ""
   for CPU. Unset order: proceed and omit next_user_pick. Partial order with the
   operator missing: require an explicit slot or refuse. Complete order with the
   operator missing: refuse.

4. ADP variant and ECR scoring param, both from resolved slots and the rec
   weight. SUPER_FLEX/OP/2+ QB slots gives adp_2qb; else rec >= 0.75 gives
   adp_ppr; 0.25 to 0.75 gives adp_half_ppr; else adp_std. There is no
   adp_2qb_ppr. Banner when rec is not exactly 1, 0.5, or 0.

Refusals, all loud, correct class each time:
- keeper/dynasty: settings.type in {1,2} or taxi_slots > 0
- unknown type: type absent or not in {0,1,2}
- IDP slot codes in the resolved list
- auction, linear, or reversal_round != 0
max_keepers > 0 on type == 0 is NOT a refusal: banner "keepers possible" and
proceed, and drop any player from a pick with a truthy is_keeper.

Also validate scoring keys against the columns the stat source provides: every
nonzero key must hit a column. Unknown keys are itemised. Classified keys with
no startable slot get one summary line. Never silent-zero anything.

Write one test per refusal condition and one per banner. This module is where a
wrong answer refuses a real draft on real draft night, so the tests are the
deliverable.

TDD: failing test first.

Finish with the four handoff steps in docs/PLAN.md section 5.
```

## Definition of done

- Every row of the §2 refusal table has a test.
- Standalone-mock path (`league_id: null`) resolves slots from the mock and
  scoring from the borrowed league, with both banners.
- Superflex fixture yields `adp_2qb` and `position=OP`.
