# S10 handoff

Eval run. Report is `evals/REPORT.md`. Worktree `feat/eval-run`.

After the report, `cli.py` changed: the poll loop still rebuilds the
board every pick; `propose` runs only when the operator is on the clock.
`--once` still asks.

## What ran

- 12 golden cases, live `recommend` / `run_stability`, five calls unless
  `coin_flip`. Cached under `evals/_cache/golden/`.
- 4 regret seats, payloads from recorded drafts + live FantasyPros.
- Operator's two seat-1 mocks transcribed and joined 140/140. Baselines
  only: Anthropic credits ran out before `recommend`.
- One `--once` cold start and one poll loop against a standalone mock
  the operator sent. They then said that was not the mock they were
  starting. The loop was killed.

## Verdict

The model separates from `argmax_vols` on `golden_forbid` and
`golden_require`. It passed 12/12. The calculator failed the three S9
cases. Read the report before changing a gate.

## Call path

Eval: `vorpal.model.recommend` and `run_stability`. A violation is the
score. Draft night `propose` was not used for the table.

```python
five = run_stability(payload, transport)  # None if coin_flip
# do not open a sixth call; recover the first raw if coin_flip
```

The runner is `evals/run.py`. `--only golden|regret|human`.

## Things the report cannot see from a skim

- **FantasyPros is row-capped at 10** on the key in this environment
  (`public_api_limited`, `tier: free`). 60 projected players (10 × 6
  positions), 10 ADP, 10 ECR. Superflex OP ADP is empty →
  `adp_1qb_market`. Paging was not probed (#18). Do not assume paid.
  Golden is not this universe; those boards are hand-written.
- **Anthropic credits died** after golden, regret, and the first live
  loop. Human 28/28 `recommend` calls failed with
  `credit balance is too low`. A new dress rehearsal will fail until
  the account has balance.
- **`te_cliff` failed `ecr_dissent`** on the first of five: rec was the
  hint and the required TE, ECR 26 vs `ecr_best` 25, flag missing. Later
  reruns set `ECR_DISAGREE`. The eval scores the first call.
- **`bye_hole` still fails almost everything.** S9's finding stands.
- **Replay is skip everywhere.** No dated projections file.
- **Human vs baselines** (28 turns): argmax 4/28, adp 2/28, ecr 6/28.
  Both drafts took Allen at 1.01; argmax took Gibbs. That comparison
  used 1QB ADP on a superflex config. Do not quote it as a superflex
  result.

## Timing (wrong mock, still a clock)

`--once` cold start: 16.9s wall, 8.3s model, 60s timer.
Live loop model: 11.8–14.6s against a 30s `pick_timer`. Poll 3s while
`drafting`. All model calls finished inside the timer.

## The page failed the human

The operator's word was clunky. The last live `board.html` put ten red
banners above a rec whose heading was the host id `8151`, a stale
proposal from pick 18 shown at pick 28, eighteen copy-paste empty-week
rows, and a table headed `delta_starter_points`. Spec asked for loud
banners. On a 30s clock they hide the pick.

S7 is correct as a snapshot test of "the numbers are on the page." It is
not a draft-night layout. Do not ship that HTML to a live pick without a
board pass. Issue: #19.

## What S10 did not do

Follow-ups are GitHub issues, not S11.

- Dress rehearsal of a mock the operator started and sat in (#21).
- Full FantasyPros list. Do not assume paid: probe paging first (#18).
- Model scoring of the two human drafts (#21, after #18).
- Draft-night board layout (#19). Dissent `why` names the hint (#20).
- Cassettes: replay by request hash, miss is an error (#24). CI scores
  golden cassettes after that (#25).
- The poll loop still rebuilds the board every pick; only `propose`
  waits for the operator's clock. Do not conflate those.
